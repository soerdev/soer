import { CalcProgressPipe } from './calc-progress.pipe';

describe('CalcProgressPipe', () => {
  it('create an instance', () => {
    const pipe = new CalcProgressPipe();
    expect(pipe).toBeTruthy();
  });
});
